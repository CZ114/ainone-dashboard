# Python
__pycache__/
*.py[cod]
*.pyc
.pytest_cache/

# Virtual environments (both legacy Scripts/ and modern bin/ styles)
backend/.venv/
backend/.venv
venv/
.venv/
.venv

# Backend
backend/claude/node_modules/
*.egg-info/
dist/
build/

# Frontend
frontend/node_modules/
frontend/.pnp
frontend/.pnp.js
frontend/dist/

# Generated / runtime state
*.db
*.db-journal
*.log
logs/
recordings/
backend/data/audio/
backend/data/csv/
backend/data/serial_logs/
extensions_state.json

# Whisper model weights — downloaded to project-local cache instead
# of ~/.cache/huggingface/hub. Per-model size: 75 MB (tiny) to 3 GB
# (large-v3); never committable. See app/extensions/whisper_local.py
# MODELS_DIR.
VoiceModel/

# Environment (contains secrets — never commit)
.env
.env.local
.env.*.local

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Build artifacts
*.tgz
*.whl

# Claude Code per-project local config (personal, auto-generated)
.claude/

# Local-only docs (kept in working tree, not pushed). Source repo
# (esp32_sensor_dashboard) is the source of truth for these.
WHISPER_LOCAL_DEBUG_JOURNEY.md
WRAPPING_NATIVE_PICKER_GUIDE.md
SPEC.md
start.sh
