# AinOne Dashboard — environment template.
#
# Copy to `.env` (gitignored) and fill in the keys you actually use.
# The Hono backend loads this on boot via Node's native
# `process.loadEnvFile` — see backend/claude/cli/node.ts:loadEnvFiles.
#
# DIARY agent secrets — referenced from agents.json env blocks as
# `${NAME}`. Anything you put here is a fallback: the diary's
# in-app Secrets manager (Settings → Diary → Secrets) takes
# precedence so a UI-pasted key always wins. Use this file when
# you want keys never to live inside backend/data/diary/agents.json.
#
# Provider                  Variable name         Get it at
# ----------------------------------------------------------------------
# Anthropic native          ANTHROPIC_KEY         console.anthropic.com/settings/keys
# DeepSeek                  DEEPSEEK_KEY          platform.deepseek.com/api_keys
# MiniMax (intl/cn)         MINIMAX_KEY           platform.minimax.io / platform.minimaxi.com
# Zhipu Z.ai (intl/cn)      ZHIPU_KEY             z.ai / open.bigmodel.cn
# Moonshot Kimi             MOONSHOT_KEY          platform.moonshot.ai
# Alibaba Qwen              QWEN_KEY              dashscope.console.aliyun.com
# Custom router/endpoint    CUSTOM_KEY            depends on your setup

# ANTHROPIC_KEY=sk-ant-...
# DEEPSEEK_KEY=sk-...
# MINIMAX_KEY=sk-...
# ZHIPU_KEY=...
# MOONSHOT_KEY=sk-...
# QWEN_KEY=sk-...
